server:
  port: 8071

spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "classpath:/config"
  rabbitmq:
    host: "localhost"
    port: 5762
    username: "guest"
    password: "guest"